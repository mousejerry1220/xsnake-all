1，Remote注解配置在接口
2，配置结果是否启用GZip压缩（或者其他方式压缩，提供压缩接口，或者内置）
3，配置请求是否启用GZip压缩（或者其他方式压缩，提供压缩接口，或者内置）
5，配置结果是否缓存
7，RMQ的队列名称必须带有应用代码，因为不同的服务不一定每台服务器都有



xsnake-server项目主要负责：jar上传分发，jar管理，服务器监控参数
xsnake-admin远程参数配置，远程各个服务器的状态显示。缓存重置
